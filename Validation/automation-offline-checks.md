設定変更検証（2026-09-17）: 26オフライン試験合格。通常実行でAstra呼出しなし、Astra管理への変更には明示理由が必要。停止フラグ維持。実モデル呼出しなし。

# 再開後の追加検証

2026-09-17 10:02 JST。24項目全PASS。停止フラグがmainとモデル呼出しの両方でCLI起動を防ぐことを追加検証。新しい3モデルの実通信完走は未実施。

以下は過去の検証記録。

# 管理スクリプトの検証

2026-09-17。`Tools/test_automation.py` の20項目が合格（モデル判定修正後）。

追加検証: 利用集計にHaikuがあっても回答本文がSonnet 5なら受理。本文のモデルが違う/欠落する場合、セッションが異なる場合、検証済み本文と最終結果が異なる場合、集計しかない旧形式の場合は拒否。これは生成したイベントを使ったオフライン試験で、修正後の実CLIストリームは次のcheckで確認する。

- 接続確認番号の照合
- 設計→Sonnet実装案→独立レビュー→指摘交換→反映の順序
- バックアップ内容と反映先
- Claude応答の抽出、モデル相違の拒否
- CLI失敗時に原本を変更しない
- 範囲外編集・不正パス・重複JSONキーの拒否
- 古いレビューの拒否
- レビュー不一致時の回数上限、修正後の再レビュー
- 作業中のユーザー編集の保持
- 初期読み取りとsnapshot間の競合検出
- 置換失敗時の原本保持
- 二重起動ロック
- タイムアウト時の親プロセス終了
- Windowsの子プロセスがタイムアウト後に書き込まないこと

実行コマンド: `python Tools/test_automation.py`。テストは一時フォルダーとダミーのモデル応答を使用し、実プロジェクトのmanifestを変更していない。
起動用 `Start-Automation.cmd --help` の実行も確認。

Astraの独立レビューで初期snapshotの競合と子プロセス残存を検出し、修正・回帰テストを追加した。

実モデルからの応答、Sonnetによる実コードレビュー、Unity統合コンパイル・HMD実機確認はこの検証には含まれない。ユーザーの環境で `Start-Automation.cmd check --timeout 90` を実行して接続を確認する。
