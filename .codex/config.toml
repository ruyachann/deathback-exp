# Project defaults; loaded by Codex only when this project is trusted.
# Does not change the model of an already running desktop conversation.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"

# Review a fixed snapshot with a separate Claude Sonnet session.
[mcp_servers.peer_claude]
command = "python"
args = ["C:/deathback/deathback-exp/Tools/peer_review_mcp.py", "--reviewer", "claude"]
tool_timeout_sec = 330
