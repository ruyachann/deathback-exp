# Claude Code担当

まず `AGENTS.md` と `Collaboration/STATE.md` を読む。

このプロジェクトのClaude指定モデルは **claude-sonnet-5**。利用不能なら止めて報告し、別モデルへ自動で代替しない。通常の計画・実装・結果確認はGPT-5.6 Solが管理する。Claudeは設計済み機能の担当実装・テスト・文書と、別セッションでの独立レビュー・指摘交換を担当する。Opus優先の相談条件はAGENTS.mdを参照する。

実装前に `Collaboration/tasks/` の指定タスクを読む。変更可能ファイル以外に問題を見つけた場合は提案として報告する。レビュー依頼ではファイルを変更しない。レビューには対象ファイルのSHA256、優先度、場所、再現条件、修正案、実行した検証、残る未確認事項を書く。

相互レビューは、最初に独立した判断を保存し、その後Solの報告を読んで照合する。自身の実装報告を独立レビューとして代用しない。SolレビューとClaudeレビューが両方得られていない状態で「相互レビュー済み」と書かない。双方の結果と必要な検証をSolが確認するまで最終受入済みとしない。

モデル選択用の起動例: `claude --model claude-sonnet-5`。既存セッションの設定が優先されることがあるので、起動時のモデル表示も確認する。
