# Claude Code担当

まず `AGENTS.md` と `Collaboration/STATE.md` を読む。

Claude と Codex は企画・計画・実装・レビューのすべてを編集できる。Claude の通常実装・レビューは **claude-sonnet-5**、企画・計画は利用可能な正確なモデル ID を確認した Opus を優先する。担当はタスクで明示し、利用不能なら報告して別モデルへ自動で代替しない。重要判断の条件は AGENTS.md を参照する。

企画・計画・実装の前に `Collaboration/tasks/` の指定タスクを読む。必要な範囲変更はタスクに記録してから編集する。レビュー依頼ではファイルを変更しない。レビューには対象ファイルのSHA256、優先度、場所、再現条件、修正案、実行した検証、残る未確認事項を書く。Codex への MCP レビュー依頼は `Collaboration/PEER_REVIEW_MCP.md` を参照する。

相互レビューは、最初に独立した判断を保存し、その後もう一方の報告を読んで照合する。自身の実装報告を独立レビューとして代用しない。Codex と Claude の独立レビューが両方得られていない状態で「相互レビュー済み」と書かない。双方の結果と必要な検証をタスク担当が確認するまで最終受入済みとしない。

モデル選択用の起動例: `claude --model claude-sonnet-5`。既存セッションの設定が優先されることがあるので、起動時のモデル表示も確認する。
