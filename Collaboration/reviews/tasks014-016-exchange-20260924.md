# task014 / 015 / 016 相互レビュー照合（2026-09-24）

照合者・計画担当: Claude Opus 5.5（`claude-opus-5-5`）。実装: task014 は GPT-5.6 Sol（`codex exec`、6回）、task015 と task016(a) は Claude Sonnet 5（`claude -p`）、task016(c) の手順書・起動スクリプトは計画担当。レビューはすべて MCP `review_with_codex`／`review_with_claude`（実装とは別セッション）。

## 経過（要約）

| 回 | task014（見た目） | task015（音とパネル） | task016（実機準備） |
| --- | --- | --- | --- |
| ビルドの自動実行で計画担当が発見 | 暗すぎる → 明るさ調整、ACES→Neutral／文字が奥行きを無視 → `LoopRoom/Text` シェーダー | パネル計測で NullReferenceException（TextMesh に MeshFilter が無い）→ MeshRenderer.localBounds／文字が視野より大きい（1.86m）→ 0.56m に縮小 | — |
| 独立レビュー | Sol request_changes（観客アバターの影が HMD に漏れる／許可ファイル欄の不整合）、Sonnet タイムアウト | Sol request_changes（計測フレームに仮の文字／固定サイズの余白）、Sonnet approve | Sol request_changes（`--autostart` が `--desktop` 必須でない、手順書の章参照、Explorer の引用）、Sonnet request_changes（Git 不在で停止、章参照、実行ポリシー） |
| 再レビュー | Sol approve、Sonnet request_changes（シェーダー target 2.0） | Sol approve、Sonnet request_changes（固定サイズで文字が縮まない） | Sol approve、Sonnet request_changes（F2 OFF だと R 案内が見えない） |
| **最終** | **Sol approve／Sonnet approve** | **Sol approve／Sonnet approve** | **Sol approve／Sonnet approve** |

不採用にした指摘と根拠（タスク文書の追修正節に記録）:
- Sonnet（task014 中）「font 再代入で素材が戻る」: `.font=` は生成時1回だけ（grep で確認）。
- Sonnet（task015 高）「素材差し替えで Color.clear が消える」: TextMesh の色は頂点カラー。変更前のコードも色→素材の順で正しく色が出ていた。
- Sonnet（task014 低2件）: 動作に影響しないため見送り。

## 最終の対象 SHA256（主要ファイル）

最終レビューの status.json（`sol-task01x-final-mcp.status.json`、`sonnet-task01x-final-mcp.status.json`）に全ファイルの SHA256 を記録。

## 検証（計画担当）

- Unity batchmode ビルド: コンパイル エラー0・警告0、シェーダーエラー0。ビルドのたびに Unity が URP・ProjectSettings 等を書き換えるため、意図した GraphicsSettings.asset（`LoopRoom/Text` の常時同梱1件）以外は元に戻した。
- 自動実行（`--desktop --autostart --autoescape`）: outcome=Escaped、elapsed=12.66、TimedOut 0、Player.log の例外0、`message panel sized (measured) bounds=(0.56, 0.16) shrink=0.30`。
- `--autostart --autoescape`（`--desktop` なし）: 30秒でセッション開始なし。
- 合成音: 実際の ProceduralAudio.cs をスタブ付きでコンパイルして波形化。clipped 0、peak ≤ 0.65、RoomTone の継ぎ目 0.0015。
- Start-DeviceCheck.ps1: Windows PowerShell 5.1 の構文解析エラー0。
- 証拠: `evidence/20260924-quality1/`（README、before-after.png、frames、text-depth-crop.png、waveforms.png、session-log）と `evidence/20260924-quality-before/`。

## 結論

**task014・015・016 は相互レビュー完了（Sol・Sonnet とも approve）、受入。** HMD 内の見え方・フレームレート・音の聞こえ方は実機（計画6、Docs/DEVICE_QUICKCHECK.md）で確認する。
