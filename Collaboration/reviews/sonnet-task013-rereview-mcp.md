# 独立レビュー: task013 追修正後の `Docs/UNITY_ACCEPTANCE.md`

## 検証対象（提供SHA256を引用）

- `Docs/UNITY_ACCEPTANCE.md` = `b1b1f081ae69374df6eafabccabc8d90b571d03fbfb26d1747bcc4f5cbd7e9e1`
- `Collaboration/tasks/013-acceptance-doc-time-limit.md` = `173827b92d516771dd8fef3b383ced696d9e6b7148bc74feb9fed471431c4ae6`（要求仕様として参照）
- 参考: `Collaboration/tasks/007-remove-session-time-limit.md` = `b68f8be11b02b8642387c07d5b881ec59d594044396390de82390bfa8c4b6fc2`（`enforcePlayLimit` 実装仕様の裏付けとして参照。本タスクの許可ファイル対象外）

## 追修正1〜3の適合確認（要求 vs 実物）

| 要求 | 該当箇所 | 判定 |
|---|---|---|
| 1. 第4章手順2からタイムアウトを外す／限定 | 「セッション進行（脱出または中断のいずれか。タイムアウトは既定では発生しない。第8章の参考手順で `enforcePlayLimit=true` にした場合のみ発生する）」 | 適合 |
| 1. 第5章の同様の記述も限定 | 「タイムアウト（既定では発生しない。第8章の参考手順で `enforcePlayLimit=true` にした場合のみ発生する）はセッション終了であり…」 | 適合 |
| 2. 第8章手順4に `timings.enforcePlayLimit=false` 確認を追加 | 手順4「あわせて `timings.enforcePlayLimit` が `false` であることを確認する。シーンに `playLimit=172` が保存されていても…使用されない」 | 適合 |
| 2. 期待される証拠・不合格基準にも追加 | 証拠「…`timings.enforcePlayLimit` の記録」／不合格基準「`timings.enforcePlayLimit` が `true` になっている」 | 適合 |
| 3. 第9章のログ説明に `enforcePlayLimit` 明記 | 「`timings`（`enforcePlayLimit` を含む）」 | 適合 |
| 180秒を合否基準にする記述が参考手順以外に残存しないか | 本文中の「180」出現は①第8章タイトルの改訂履歴注記（撤廃した旨の記述）、②「参考: 時間上限を再導入した場合の確認手順」内の2箇所のみ。いずれも現行の合否基準としては使われていない | 適合（合否基準としての180秒残存なし） |

## 指摘事項

該当なし（Blocker/Major/Minor いずれも検出せず）。

強いて挙げるなら以下は**指摘ではなく既存仕様への言及**として記録する。

- **Info**: 第1章に「準備待ちのタイムアウト後も Floor が確定したままなら…」という記述があり、`LoopRules.enforcePlayLimit` によるセッションタイムアウトとは無関係な別概念（XR初期化待ちのタイムアウト、`DemoRig` 側）である。同じ「タイムアウト」という語を使っているため読者が混同しうるが、この記述はtask013の設計範囲（180秒/`enforcePlayLimit`関連の文言）には含まれておらず、追修正の対象外であるため今回の変更としては問題ない。将来的な用語整理の余地としてのみ記録する。

## 未確認事項（unverified checks）

1. **git diffによる変更範囲の確認（第0・2・4・5・8・9章のみに限定されているか）**: 本レビューはスナップショット（変更後の完成形）のみを対象としており、変更前バージョンとの `git diff` は実行していない。差分ツール・変更前ファイルが手元にないため、「空白以外の変更が指定章だけであること」は内容の整合性からの推測に留まり、確定的な検証はできていない。
2. **第1章冒頭の注記（F1）・遷移表示の注記が変更されていないこと**: 同様に変更前バージョンが手元にないため、文言が一字一句変わっていないかの直接比較は未実施。読んだ限り第1章の内容は `enforcePlayLimit`/180秒とは無関係な記述であり不審な変更の痕跡は見当たらないが、diffによる裏付けは取れていない。
3. task013.md 自身が明記する通り、証拠画像と変更前後の照合（`evidence/20260924-task013/` と `git diff` による確認）は本レビューのスナップショットに含まれない構成上の制約があり、意図的に対象外とされている。
4. Unity Editorでの実機能検証（コンパイル、実際のPlay挙動、`enforcePlayLimit=true` 参考手順の動作確認）は本レビューでは未実施（本レビューはドキュメントレビューであり、指示通りテスト実行は行っていない）。

## 判定

**approve**（承認）

追修正で要求された1〜3の全項目がテキストレベルで正確に反映されており、180秒を合否基準とする記述の残存も確認されなかった。ただし、git diffによる変更範囲の機械的確認および第1章の非改変確認は本レビューの範囲外（前バージョン未提供）であるため、上記「未確認事項」として別途照合されたい。