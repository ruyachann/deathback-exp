# task019 相互レビュー照合（2026-09-24）

照合者・計画担当: Claude Opus 5.5。実装: GPT-5.6 Sol（2回）。レビュー: MCP 経由の Sol と Sonnet（別セッション）。

- 独立: Sol request_changes（観客から扉と時計が消えて不自然、UpdatePublic が active を無視）／Sonnet request_changes（扉が既定レイヤーでない）。
- 計画担当の判断: 動く扉は開くタイミングが手がかりなのでレイヤー8のまま（実装者の判断を承認）、観客専用レイヤー9に閉じた扉を同じ位置に追加。時計の外装は既定、文字だけ8。UpdatePublic の件は以前からの挙動として後の課題へ。
- 最終（RoomVisuals.cs `1ab59ecd…`）: **Sol approve／Sonnet approve**。Sonnet が未確認とした「HMD がレイヤー9を描かないこと」は、計画担当が `DemoRig.cs:87`（`View.cullingMask = ~(1 << 9)`）で確認した。Sonnet の1回目の再レビューは、使えないツールを呼ぼうとして判定のない出力になったため再依頼した。

## 検証（計画担当）

- ビルド エラー0・警告0、例外0。証拠 `evidence/20260924-oneroom/`（before-after.png、frames f00〜f04）。
- 自動実行は約10秒で「Interrupted」になり、その後ウィンドウが最小化された。desktop モードの「フォーカスを失ったら中断」の規則による（自動実行中に他のウィンドウがフォーカスを取った）。ゲームの不具合ではないが、自動の証拠撮影には支障 → 後の課題。
- Player.log: 天井の点光源の影が6面の影マップを使い、影アトラスに収めるため解像度を下げたという警告。VR の負荷の観点で実機確認の項目に加える（必要ならスポットライトに替える）。

## 結論

**task019 は相互レビュー完了（Sol・Sonnet とも approve）、受入。** HMD 内の見え方・負荷、観客画面の見え方（VR 時のみ有効）は実機で確認。
