# LoopRoom: AI分担と受入規則

**停止制御**: `Collaboration/PAUSE.json` がある間は新しいモデル呼出し・実装を始めない。ユーザーの明示的な再開指示後、`Collaboration/RESUME.md` を読み、利用枠と保存版を確認してから再開する。

ユーザー指定（2026-09-23更新）: **Codex と Claude は企画・計画・実装・レビューのいずれも編集できる**。企画・計画と重要判断は GPT-6 Astra (`gpt-6-astra`) または利用可能と確認した Claude Opus を優先し、実装と通常の検証は GPT-5.6 Sol (`gpt-5.6-sol`) または Claude Sonnet 5 (`claude-sonnet-5`) を優先する。担当はタスクごとに決め、モデルを黙って切り替えない。Opus の正確なモデル ID と利用権は実行前に確認する。

## 作業の進め方

1. 企画・計画の担当（Astra または確認済み Opus）が `Collaboration/tasks/` に目的、担当、許可ファイル、受入条件、リスクを記す。どちらの製品も企画・計画を提案・修正できる。
2. 指定した実装担当（Sol または Sonnet）がタスク範囲を実装する。両製品に同じファイルの編集権があるが、同時編集はタスク単位で調整する。
3. 実装者と異なるセッションの Codex と Claude が、同じ SHA256 のソースを先に独立レビューする。双方の報告を交換し、指摘ごとの対応と最終判定を残す。MCP 経由の依頼方法は `Collaboration/PEER_REVIEW_MCP.md` を参照する。
4. 修正後は変更範囲を再レビューする。タスク担当が双方の判定と必要な検証を確認し、結果報告と次計画を記録する。Unity/実機未検証は明記する。
5. `Collaboration/STATE.md` に状況と次の担当を保存。過去のレビューは証跡として維持する。

## Opus優先の相談条件

- 体験の核、2mの活動範囲、身体位置の扱い、安全性、システム構成を大きく変更する重要判断。
- 原因調査や最大2案の修正でも行き詰まり、Sol/Sonnetの見解が解消しない場合。
- 許可範囲を超える変更や複数の選択肢に重大な影響があり、通常判断では決められない場合。

相談時は問題・試したこと・双方の見解・選択肢・判断してほしい点を保存し、その判断だけをOpusへ依頼する。Opusが利用不可と確認できた場合は理由を記録してAstraへ渡す。通常の設計、レビュー、最終報告のたびには呼ばない。認証失敗や制限は復旧待ちとして記録し、Astraを代替実装モデルにしない。

## 権限・正確性

- `C:\deathback\deathback-exp` が新しい正本。旧outputs/LoopRoomUnityは移行元の保存版。開いている旧Unityのユーザー編集を保護し、旧版を新正本の実機検証済みと扱わない。
- 利用不可・タイムアウトは「未実施」と記録。指示ファイルを保存しただけで実行モデルの切替や相互レビューが完了したとは扱わない。
- この分担は役割の指定であり、権限の拡大ではない。ネットワーク制限、プロキシ、承認機構を回避しない。認証情報や個人の設定を報告に含めない。
- Library / Temp / PackageCacheの修正禁止。今後の依存追加・削除・更新はUnityEditor.PackageManager.Client APIを使用し、manifestとlockfileはUnityに解決させる。保存済みtask001の管理スクリプトは過去の限定されたmanifest変更の証跡であり、今後の依存更新の一般的な実行方法にはしない。
- ユーザーがUnityを開いている間は同じプロジェクトを別Editorで開かない。既存のシーン・ユーザー編集を上書きしない。
- Unity統合コンパイル、独立モデルテスト、構文検査、実機確認を区別して報告する。

## 体験の不変条件

- Quest 3のPCVR、2m四方。（2026-09-23 ユーザー決定: 180秒の時間上限は当面外し、まずしっかり遊べる体験を優先する。再導入はユーザー判断。）
- 死亡時の強制姿勢変更、周回ごとのXR原点移動をしない。
- 即死/暗転/復帰の因果順、旧周回入力の拒否、死亡処理の一意性を維持。
- 観客表示に攻略操作を漏らさない。HMDでの見え方・到達範囲は実機受入項目。

## 段階・Git・利用枠

- 現在の範囲と受入基準はDocs/Plans/STAGE_PLAN.md。担当開始時にCollaboration/STATE.mdを読む。
- Codex と Claude の双方が Unity/ランタイム、テスト、企画、計画、文書を編集できる。担当ファイルは各タスクで定め、同じファイルの並列編集を避ける。実装者とは別セッションのSol/Sonnetが独立レビュー後、双方の報告を交換する。
- 作業ブランチでcommit/push、Issueに結び付くドラフトPRを作る。mergeはユーザー指示後。既存履歴を保護する。
- Codexの5時間枠はアカウント共通。Claude枠は別に記録し、不明ならAPI費用から推定しない。10%未満で新規AI呼出しを止め、進捗・対象SHA・残課題・再開コマンドを保存。残量取得できない場合はユーザー確認を待つ。
- Claude連携前にCollaboration/history/SONNET_401_ROOT_CAUSE.mdとCLAUDE_CONNECTION_CHECK.mdを読む。401と通信拒否を区別し、権限・プロキシを迂回しない。認証情報を保存/pushしない。
